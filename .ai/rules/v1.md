---
paths:
  - app/Http/Controllers/Api/V1/CapabilityController.php
---

# V1

## Publish canonical woodworking schemas to agents
Keep `/api/v1/capabilities` synchronized with every supported part operation and its enum values. Increment `schema_version` when the advertised contract changes so MCP/API agents can discover new editor tools without relying on source-code knowledge.
